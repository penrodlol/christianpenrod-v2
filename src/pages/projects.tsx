import { Layout } from '@components/Layout';
import { NextPage } from 'next';

const Projects: NextPage = () => {
  return <Layout title="Projects" subTitle="What has Christian done?"></Layout>;
};

export default Projects;
